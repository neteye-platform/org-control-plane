name: neteye-keycloak
description: NetEye Keycloak image
topics:
  - neteye
  - container
  - docker
  - identity-management
  - keycloak
  - oidc
is_template: false
archived: false
template:
  owner: neteye-platform
  repository: default-repository-template
  include_all_branches: false

branch_rulesets:
  main-branch-protection:
    rules:
      required_status_checks:
        required_check:
          - context: "Run theme tests"
            integration_id: 15368 # GH Actions
          - context: "Run plugin tests"
            integration_id: 15368 # GH Actions
